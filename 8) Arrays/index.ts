const superHeros: string[] = []
const heroPower: number[] = []

// we can also define like this
const heroFans:Array<number>  = []

type User={
    name:string,
    isActive:boolean
}

const allUsers:Array<User> = []


superHeros.push("spiderman")
heroPower.push(2)

const mlModel:number[][] = [
    [255,255,255],
    [121,255,231],
]

allUsers.push({name:"rishi",isActive:true})
allUsers.push({name:"carlos",isActive:true})
allUsers